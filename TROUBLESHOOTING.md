# 🔧 Troubleshooting - Переводы не работают

**Дата:** 28 февраля 2026  
**Проблема:** Тексты не переводятся при переключении языка

---

## ✅ Что добавлено для отладки

### 1. Debug режим в i18n
**Файл:** `src/i18n/config.ts`
- Включен `debug: true`
- Добавлены console.log при инициализации

### 2. Логи в I18nProvider
**Файл:** `src/components/I18nProvider.tsx`
- Логи сохраненного языка
- Логи переключения языка

### 3. Логи в LanguageSwitcher
**Файл:** `src/components/LanguageSwitcher.tsx`
- Логи при клике на язык
- Подтверждение смены языка

---

## 🔍 Как диагностировать

### Шаг 1: Проверьте консоль браузера

Откройте DevTools (F12) и проверьте:

```
🌍 i18n initialized: { language: 'en', languages: ['en', 'hy', 'ru'], keysCount: 13 }
🔄 I18nProvider: saved language = null , current = en
```

**Если этого НЕТ:**
- Проблема с инициализацией i18n
- Проверьте ошибки в консоли
- Проверьте что файлы en.json, hy.json, ru.json существуют

### Шаг 2: Переключите язык

Нажмите на Globe icon 🌐 и выберите язык.

**Должны увидеть:**
```
🔄 LanguageSwitcher: Changing language from en to ru
✅ Language changed to: ru
```

**Если этого НЕТ:**
- LanguageSwitcher не подключен
- Проверьте что он есть в Navbar

### Шаг 3: Проверьте localStorage

В консоли выполните:
```javascript
localStorage.getItem('i18nextLng')
```

**Должно вернуть:** `"ru"` или `"hy"` или `"en"`

**Если null:**
- i18n не сохраняет выбор
- Проблема с конфигурацией detection

### Шаг 4: Проверьте что t() работает

В консоли выполните:
```javascript
window.i18n.t('common.save')
```

**Должно вернуть:** 
- `"Save"` (если язык en)
- `"Պահպանել"` (если язык hy)
- `"Сохранить"` (если язык ru)

**Если возвращает ключ:**
- Ключ не найден в JSON
- Проверьте что ключ существует

---

## 🐛 Частые проблемы

### Проблема 1: "i18n is not initialized"

**Причина:** I18nProvider не подключен в layout

**Решение:**
1. Откройте `src/app/layout.tsx`
2. Убедитесь что есть `<I18nProvider>`
3. Проверьте что children обернуты

### Проблема 2: Переводы не меняются

**Причина:** Компонент не использует `t()`

**Решение:**
1. Проверьте что в компоненте есть `const { t } = useTranslation()`
2. Проверьте что тексты обернуты в `{t('key')}`
3. Проверьте что ключ правильный

### Проблема 3: LanguageSwitcher не отображается

**Причина:** Не подключен в Navbar

**Решение:**
1. Откройте `src/components/layout/Navbar.tsx`
2. Убедитесь что импортирован `LanguageSwitcher`
3. Убедитесь что `<LanguageSwitcher />` в рендере

### Проблема 4: Hydration errors

**Причина:** SSR/Client mismatch

**Решение:**
1. Проверьте что в config.ts есть `lng: 'en'` для SSR
2. Проверьте что `useSuspense: false`
3. Очистите кэш браузера

---

## 🔧 Быстрые исправления

### Исправление 1: Полная перезагрузка

```bash
# Остановите сервер (Ctrl+C)
# Очистите .next
rm -rf .next

# Перезапустите
npm run dev
```

### Исправление 2: Очистка localStorage

В консоли браузера:
```javascript
localStorage.clear()
location.reload()
```

### Исправление 3: Принудительная смена языка

В консоли браузера:
```javascript
window.i18n.changeLanguage('ru')
```

Затем перезагрузите страницу.

---

## 📋 Checklist диагностики

- [ ] Файлы переводов существуют (en.json, hy.json, ru.json)
- [ ] В консоли видно "i18n initialized"
- [ ] I18nProvider в layout.tsx
- [ ] LanguageSwitcher в Navbar
- [ ] При клике на язык видны логи
- [ ] localStorage сохраняет язык
- [ ] window.i18n.t() возвращает переводы
- [ ] Компоненты используют useTranslation()
- [ ] Тексты обернуты в t()

---

## 💡 Если ничего не помогает

1. **Скопируйте все логи из консоли**
2. **Укажите на какой странице проблема**
3. **Скриншот LanguageSwitcher**
4. **Результат window.i18n.t('common.save')**

И я найду проблему!

---

**Создано:** AI Assistant  
**Дата:** 28.02.2026
