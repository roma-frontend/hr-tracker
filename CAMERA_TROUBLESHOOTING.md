# 📹 Camera Troubleshooting Guide

## Проблема: "Camera not active" или камера не запускается

### Возможные причины и решения:

### 1️⃣ Разрешения браузера

**Chrome/Edge:**
1. Нажмите на иконку замка/камеры слева от адресной строки
2. Найдите "Камера" или "Camera"
3. Выберите "Разрешить" (Allow)
4. Обновите страницу (F5)

**Или через настройки:**
1. Откройте `chrome://settings/content/camera` (Chrome) или `edge://settings/content/camera` (Edge)
2. Убедитесь, что камера не заблокирована
3. Добавьте `http://localhost:3000` в список разрешенных сайтов

**Firefox:**
1. Нажмите на иконку камеры в адресной строке
2. Выберите "Разрешить доступ к камере"
3. Обновите страницу

### 2️⃣ Камера занята другим приложением

Закройте все программы, которые могут использовать камеру:
- Zoom, Teams, Skype
- Discord, OBS Studio
- Другие вкладки браузера с камерой
- Приложения Windows Camera

**Проверка (Windows):**
```powershell
# Откройте "Параметры" → "Конфиденциальность" → "Камера"
# Убедитесь, что доступ к камере разрешен
```

### 3️⃣ Системные разрешения (Windows)

1. Откройте **Параметры Windows** (Win + I)
2. Перейдите в **Конфиденциальность и безопасность** → **Камера**
3. Убедитесь, что:
   - "Доступ к камере" включен
   - "Разрешить приложениям доступ к камере" включен
   - "Разрешить классическим приложениям доступ к камере" включен

### 4️⃣ HTTPS требование

Некоторые браузеры требуют HTTPS для доступа к камере.

**Решение:** Используйте `localhost` (уже используется ✅)
- `http://localhost:3000` - должно работать
- `http://127.0.0.1:3000` - также должно работать

### 5️⃣ Проверка наличия камеры

Откройте консоль разработчика (F12) и выполните:

```javascript
navigator.mediaDevices.enumerateDevices()
  .then(devices => {
    const cameras = devices.filter(d => d.kind === 'videoinput');
    console.log('Cameras found:', cameras);
  });
```

Если камер нет в списке:
- Проверьте подключение камеры (USB, встроенная)
- Переподключите USB камеру
- Обновите драйверы камеры

### 6️⃣ Очистка кэша браузера

1. Откройте DevTools (F12)
2. Правой кнопкой на кнопку обновления
3. Выберите "Очистить кэш и жесткая перезагрузка"

### 7️⃣ Проверка через DevTools Console

Откройте консоль (F12) и посмотрите на ошибки при нажатии "Start Camera":

**Возможные ошибки:**

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `NotAllowedError` | Доступ запрещен | Разрешите камеру в настройках браузера |
| `NotFoundError` | Камера не найдена | Подключите камеру |
| `NotReadableError` | Камера занята | Закройте другие приложения |
| `OverconstrainedError` | Неподходящие параметры | Камера не поддерживает 640x480 |

### 8️⃣ Тест камеры

Проверьте, работает ли камера в других приложениях:
- Windows Camera app
- https://webcamtests.com/
- Zoom/Skype test call

### 9️⃣ Проверка localhost

Убедитесь, что вы открыли приложение на:
- ✅ `http://localhost:3000/settings`
- ✅ `http://localhost:3001/settings`
- ❌ НЕ `http://192.168.x.x:3000` (не сработает без HTTPS)

### 🔟 Антивирус/Firewall

Некоторые антивирусы блокируют доступ к камере:
- Временно отключите антивирус
- Добавьте браузер в исключения

---

## 🔍 Debug режим

Откройте консоль (F12) перед тем как нажать "Start Camera".

Вы должны увидеть:
```
🎥 Requesting camera access...
✅ Camera access granted
✅ Video metadata loaded, starting detection
```

Если видите `❌ Error accessing webcam:`, прочитайте текст ошибки для диагностики.

---

## ✅ После исправления

1. Обновите страницу (F5)
2. Нажмите "Start Camera"
3. Разрешите доступ во всплывающем окне браузера
4. Камера должна запуститься!

---

## 💡 Быстрая проверка

**Checklist:**
- [ ] Камера подключена и работает в других приложениях
- [ ] Браузер Chrome/Edge/Firefox (не Safari на Windows)
- [ ] URL начинается с `http://localhost:`
- [ ] Разрешения камеры даны в браузере
- [ ] Разрешения камеры даны в Windows
- [ ] Камера не используется другим приложением
- [ ] Консоль браузера открыта для просмотра ошибок

Если всё еще не работает, откройте консоль (F12) и скопируйте текст ошибки!
