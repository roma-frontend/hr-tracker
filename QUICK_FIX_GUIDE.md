# 🔧 Быстрое исправление проблем

## Проблема 1: "Your account is pending admin approval"

### Причина:
Существующие пользователи в базе данных не имеют флага `isApproved: true`

### Решение:

**Способ 1: Через браузер (Самый простой)** ⭐

1. Откройте http://localhost:3000 в браузере
2. Нажмите `F12` (откроется DevTools)
3. Перейдите на вкладку **Console**
4. Вставьте и выполните эту команду:

```javascript
fetch('/api/approve-all', { method: 'POST' })
  .then(r => r.json())
  .then(result => {
    console.log('✅ Результат:', result);
    alert('Все пользователи одобрены! Перезагрузите страницу.');
  });
```

5. Перезагрузите страницу (`F5`)
6. Войдите в систему

---

**Способ 2: Через Convex Dashboard**

1. Откройте https://dashboard.convex.dev
2. Выберите ваш проект
3. Перейдите в раздел **Functions**
4. Найдите функцию `migrations:approveAllExistingUsers`
5. Нажмите **Run**
6. В поле аргументов введите: `{}`
7. Нажмите **Submit**

---

## Проблема 2: Navbar показывает "User" вместо имени пользователя

### Причина:
Сессия не загружается автоматически при старте приложения

### Решение:
Уже исправлено! Теперь:
- При монтировании компонента `Providers` автоматически загружается сессия из cookies
- Данные пользователя сохраняются в Zustand store
- Navbar и Sidebar отображают корректную информацию

### Проверка:
1. Откройте DevTools (`F12`)
2. Перейдите на вкладку **Console**
3. Должно быть сообщение: `Session loaded: { userId: ..., name: ..., ... }`

---

## Проблема 3: Страницы Employees и Approvals не видны в сайдбаре

### Причина:
Фильтрация навигации по ролям не работала корректно

### Решение:
Уже исправлено! Теперь навигация фильтруется по ролям:

| Страница | Admin | Supervisor | Employee |
|----------|-------|------------|----------|
| Dashboard | ✅ | ✅ | ✅ |
| Leaves | ✅ | ✅ | ✅ |
| Calendar | ✅ | ✅ | ✅ |
| **Employees** | ✅ | ✅ | ❌ |
| **Reports** | ✅ | ✅ | ❌ |
| **Approvals** | ✅ | ❌ | ❌ |
| Settings | ✅ | ✅ | ✅ |

---

## 🚀 Полная последовательность действий:

### 1. Одобрите всех пользователей:
```javascript
// В консоли браузера на http://localhost:3000
fetch('/api/approve-all', { method: 'POST' })
  .then(r => r.json())
  .then(console.log);
```

### 2. Перезагрузите страницу:
- Нажмите `F5` или `Ctrl+R`

### 3. Войдите в систему:
- Email: `romangulanyan@gmail.com` (получит роль admin)
- Или любой другой email (получит роль employee, но требует одобрения)

### 4. Проверьте Navbar:
- Должно отображаться ваше имя
- Если есть аватар - должен отображаться

### 5. Проверьте Sidebar:
- Для админа должны быть видны все пункты меню
- Включая **Employees** и **Approvals**

### 6. Протестируйте систему одобрения:
- Откройте инкогнито окно
- Зарегистрируйте нового пользователя
- Вернитесь в админку
- Перейдите на `/approvals`
- Одобрите нового пользователя

---

## 🐛 Отладка

### Проверка сессии:
```javascript
// В консоли браузера
fetch('/api/session')
  .then(r => r.json())
  .then(console.log);
```

### Проверка текущего пользователя в store:
```javascript
// В консоли браузера (если есть доступ к window.__NEXT_DATA__)
console.log(localStorage.getItem('hr-auth-storage'));
```

### Проверка cookies:
```javascript
// В консоли браузера
console.log(document.cookie);
```

---

## 📝 Измененные файлы:

1. `src/components/layout/Providers.tsx` - автозагрузка сессии
2. `src/store/useAuthStore.ts` - добавлен метод `login()`
3. `convex/migrations.ts` - функция одобрения пользователей
4. `src/app/api/approve-all/route.ts` - API endpoint для одобрения

---

## ✅ Контрольный список:

- [x] Исправлена загрузка сессии
- [x] Добавлен метод `login()` в store
- [x] Создана функция миграции для одобрения пользователей
- [x] Создан API endpoint `/api/approve-all`
- [x] Исправлен тип роли: `manager` → `supervisor`
- [x] Навигация фильтруется по ролям
- [x] Аватары отображаются в Navbar и Sidebar

---

**Дата:** 2026-02-23  
**Версия:** 2.1
