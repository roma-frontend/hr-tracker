# 🚀 Новые возможности Stripe - Быстрая справка

## ⚡ Новые команды (быстрый старт)

```bash
# 📈 График роста подписок
npm run stripe:growth-chart
# → Создаёт PNG график показывающий рост подписок

# 📄 Экспорт в PDF
npm run stripe:export-pdf
# → Создаёт красивый PDF отчёт

# 🔄 Синхронизация с Stripe
npm run stripe:sync
# → Импортирует данные из реального Stripe API

# ⏰ Проверка trial периодов
npm run stripe:check-trials
# → Показывает подписки с заканчивающимся trial
```

## 🌐 Веб Dashboard

```bash
npm run dev
# Открыть: http://localhost:3000/admin/stripe-dashboard
```

**Возможности:**
- 📊 Real-time метрики (Total, MRR, Growth, Trials)
- 📈 Графики по статусам и планам
- 📋 Таблица последних подписок
- 🔄 Refresh и Export кнопки

---

## 📊 Сравнение форматов экспорта

| Формат | Команда | Когда использовать |
|--------|---------|-------------------|
| **Excel** | `stripe:export` | Для бухгалтерии, анализа в Excel |
| **PDF** | `stripe:export-pdf` | Для презентаций, отправки email |
| **График** | `stripe:growth-chart` | Для визуализации трендов |
| **Терминал** | `stripe:view` | Быстрая проверка статуса |

---

## 🎯 Типичные задачи

### Утренняя проверка
```bash
npm run stripe:check-trials  # Кому нужны напоминания?
npm run stripe:view          # Общая картина
```

### Еженедельный отчёт
```bash
npm run stripe:export-pdf
# Отправить PDF боссу/инвестору
```

### Синхронизация данных
```bash
# 1. Добавить в .env.local:
STRIPE_SECRET_KEY=sk_test_your_key

# 2. Запустить:
npm run stripe:sync
```

### Презентация для инвесторов
```bash
npm run stripe:growth-chart   # График роста
npm run stripe:export-pdf     # PDF отчёт
# Открыть dashboard в браузере
```

---

## 📁 Созданные файлы

После запуска команд в папке проекта появятся:

```
Desktop/office/
├── stripe-growth-chart-[timestamp].png    ← График
├── stripe-report-[timestamp].pdf           ← PDF отчёт
└── stripe-transactions-[timestamp].xlsx    ← Excel
```

---

## ⚙️ Настройка (один раз)

### Для синхронизации с реальным Stripe

1. **Получить API ключ:**
   - Зайти: https://dashboard.stripe.com/apikeys
   - Скопировать Secret key

2. **Добавить в `.env.local`:**
   ```bash
   STRIPE_SECRET_KEY=sk_test_51...
   ```

3. **Запустить синхронизацию:**
   ```bash
   npm run stripe:sync
   ```

### Для автоматических напоминаний

**Windows Task Scheduler:**
- Программа: `npm`
- Аргументы: `run stripe:check-trials`
- Папка: `C:\Users\...\Desktop\office`
- Расписание: Ежедневно в 9:00

**Linux/Mac crontab:**
```bash
0 9 * * * cd /path/to/office && npm run stripe:check-trials
```

---

## 💡 Полезные советы

### Быстрый доступ к метрикам
```bash
# Создать alias (bash/zsh)
alias stripe-status="cd ~/Desktop/office && npm run stripe:view"
alias stripe-check="cd ~/Desktop/office && npm run stripe:check-trials"

# Теперь можно просто:
stripe-status
stripe-check
```

### Экспорт в нужный формат
```bash
# Для анализа данных → Excel
npm run stripe:export

# Для презентаций → PDF
npm run stripe:export-pdf

# Для визуализации → График
npm run stripe:growth-chart
```

### Мониторинг trial подписок
```bash
# Каждый день проверять
npm run stripe:check-trials

# Если есть CRITICAL (≤3 дня):
# → Отправить email с напоминанием
# → Предложить скидку или продление
```

---

## 🆘 Troubleshooting

### График не создаётся
```bash
# Проверить установку зависимостей
npm install chartjs-node-canvas
```

### PDF ошибка
```bash
# Переустановить pdfkit
npm install pdfkit
```

### Синхронизация не работает
```bash
# Проверить API ключ в .env.local
cat .env.local | grep STRIPE_SECRET_KEY

# Должно быть:
STRIPE_SECRET_KEY=sk_test_... или sk_live_...
```

### Dashboard не открывается
```bash
# Запустить dev сервер
npm run dev

# Открыть правильный URL
http://localhost:3000/admin/stripe-dashboard
```

---

## 📚 Полная документация

- **Краткая справка**: `STRIPE_QUICK_START.md`
- **Расширенные функции**: `STRIPE_ADVANCED_FEATURES.md` ⭐
- **Все команды**: `STRIPE_COMMANDS.md`
- **Настройка**: `STRIPE_SETUP_COMPLETE.md`
- **Шпаргалка**: `STRIPE_CHEATSHEET.md`

---

## ✨ Что нового?

| Функция | Статус | Описание |
|---------|--------|----------|
| График роста | ✅ Готово | PNG изображение с трендами |
| PDF экспорт | ✅ Готово | Профессиональный отчёт |
| Stripe sync | ✅ Готово | Импорт из реального API |
| Web Dashboard | ✅ Готово | Интерактивный интерфейс |
| Trial reminders | ✅ Готово | Система напоминаний |

---

**Начни с проверки trial подписок:**
```bash
npm run stripe:check-trials
```

Успехов! 🎉
