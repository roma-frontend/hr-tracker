# 💳 Stripe - Список команд

## Основные команды

### 📊 Экспорт в Excel (РЕКОМЕНДУЕТСЯ)
```bash
npm run stripe:export
```
**Результат:** Создаёт Excel файл `stripe-transactions-[timestamp].xlsx` и автоматически открывает его

**Содержимое:**
- Лист 1: Все подписки с полной информацией
- Лист 2: Статистика (количество, MRR и т.д.)

---

### 🖥️ Просмотр в терминале
```bash
npm run stripe:view
```
**Результат:** Красивый цветной вывод всех подписок в консоли

**Показывает:**
- Все подписки с деталями
- Статусы с цветовой кодировкой
- Сводную статистику
- MRR от активных подписок

---

### 🧪 Добавить тестовые данные
```bash
npm run stripe:add-test-data
```
**Результат:** Добавляет 4 тестовые подписки для демонстрации

**Тестовые подписки:**
- 1 Professional (активная)
- 1 Starter (пробная)
- 1 Enterprise (активная)
- 1 Professional (отменённая)

---

## Примеры использования

### Быстрый Excel отчёт
```bash
cd Desktop/office
npm run stripe:export
```
✅ Файл создан и открыт автоматически!

### Проверка в терминале
```bash
cd Desktop/office
npm run stripe:view
```
✅ Видите сразу сколько активных подписок и MRR!

### Полный цикл тестирования
```bash
cd Desktop/office
npm run stripe:add-test-data    # Добавить тестовые данные
npm run stripe:view             # Посмотреть в терминале
npm run stripe:export           # Экспортировать в Excel
```

---

## Что показывается

### В Excel файле (16 колонок):
1. № п/п
2. Email
3. План (Starter/Professional/Enterprise)
4. Статус (Активная/Пробная/Отменена и т.д.)
5. Цена/месяц ($)
6. Stripe Customer ID
7. Subscription ID
8. Session ID
9. Organization ID
10. User ID
11. Начало периода
12. Конец периода
13. Конец пробного
14. Отменить в конце (Да/Нет)
15. Дата создания
16. Дата обновления

### В терминале:
- План с эмодзи (🚀 Starter, 💼 Professional, 🏢 Enterprise)
- Статус с цветами (✅ Active, 🆓 Trialing, ❌ Canceled и т.д.)
- Email пользователя
- Все Stripe ID
- Периоды подписки
- Сводная статистика

---

## Расположение файлов

### Скрипты:
- `scripts/export-stripe-to-excel.ts` - Экспорт в Excel
- `scripts/view-stripe-transactions.ts` - Просмотр в терминале
- `scripts/add-test-stripe-data.ts` - Добавление тестовых данных

### Convex функции:
- `convex/subscriptions.ts` → `listAll` - Получение всех подписок
- `convex/subscriptions.ts` → `upsertSubscription` - Добавление/обновление

### Документация:
- `STRIPE_QUICK_START.md` - Быстрый старт
- `STRIPE_VIEWER_README.md` - Полное руководство
- `STRIPE_COMMANDS.md` - Этот файл

---

## Troubleshooting

### ❌ "CONVEX_URL not found"
**Решение:** Проверьте `.env.local`:
```
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
```

### ❌ "Cannot find module"
**Решение:** 
```bash
npm install
```

### ⚠️ "No subscriptions found"
**Решение:** Добавьте тестовые данные:
```bash
npm run stripe:add-test-data
```

### ❌ Excel файл не открывается автоматически
**Решение:** Откройте вручную из папки `Desktop/office/`

---

## Цены планов (для расчёта MRR)

| План | Цена/месяц |
|------|------------|
| Starter | $0 (бесплатно) |
| Professional | $49 |
| Enterprise | $199 |

**MRR** = Сумма цен всех активных подписок
