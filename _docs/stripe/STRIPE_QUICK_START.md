# 💳 Stripe Транзакции - Быстрый старт

## 📊 Получить список в Excel

Просто выполните одну команду:

```bash
cd Desktop/office
npm run stripe:export
```

✅ Готово! Excel файл будет создан в папке `Desktop/office/`

---

## 🖥️ Просмотр в терминале (красивый вывод)

```bash
cd Desktop/office
npm run stripe:view
```

---

## 🧪 Добавить тестовые данные

Если база данных пустая, добавьте тестовые подписки:

```bash
cd Desktop/office
npm run stripe:add-test-data
```

---

## 📋 Все доступные команды

| Команда | Описание |
|---------|----------|
| `npm run stripe:export` | 📊 Экспорт в Excel файл |
| `npm run stripe:view` | 🖥️ Красивый просмотр в терминале |
| `npm run stripe:add-test-data` | 🧪 Добавить тестовые данные |

---

## 📊 Что содержит Excel файл?

### Лист 1: Подписки
- № п/п
- Email пользователя
- План (Starter/Professional/Enterprise)
- Статус (Активная/Пробная/Отменена и т.д.)
- Цена в месяц ($)
- Stripe Customer ID
- Subscription ID
- Session ID
- Organization ID
- User ID
- Начало периода
- Конец периода
- Конец пробного периода
- Отменить в конце периода (Да/Нет)
- Дата создания
- Дата обновления

### Лист 2: Статистика
- Всего подписок
- По статусам (Активные, Пробные, Просрочены, Отменены)
- По планам (Starter, Professional, Enterprise)
- MRR (Monthly Recurring Revenue) от активных подписок

---

## 🎨 Цветовая кодировка в терминале

**Статусы:**
- 🆓 Пробный период (голубой)
- ✅ Активная (зелёный)
- ⚠️ Просрочена (жёлтый)
- ❌ Отменена (красный)
- ⏳ Не завершена (фиолетовый)

**Планы:**
- 🚀 Starter (синий)
- 💼 Professional (фиолетовый)
- 🏢 Enterprise (жёлтый)

---

## 💡 Примеры использования

### Пример 1: Быстрый отчёт для бухгалтерии
```bash
npm run stripe:export
```
Откройте созданный файл в Excel и отправьте бухгалтеру.

### Пример 2: Проверка статуса подписок
```bash
npm run stripe:view
```
Увидите сразу сколько активных, отменённых и пробных подписок.

### Пример 3: Тестирование
```bash
npm run stripe:add-test-data
npm run stripe:export
```
Добавляет тестовые данные и создаёт Excel отчёт.

---

## 🔧 Требования

- Node.js установлен
- Переменная `NEXT_PUBLIC_CONVEX_URL` в `.env.local`
- Convex подключён к проекту

---

## ❓ Проблемы?

### Ошибка: "CONVEX_URL not found"
Проверьте `.env.local`:
```
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
```

### Ошибка: "Cannot find module"
Установите зависимости:
```bash
npm install
```

### Нет данных / пустой список
Добавьте тестовые данные:
```bash
npm run stripe:add-test-data
```
