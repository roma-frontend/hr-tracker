// ──── BEFORE (Russian strings) ────────────────────────────────
// Line 289: issues.push(`${enrichedTickets.length} критических тикетов`);
// Line 294: issues.push(`${enrichedIncidents.length} активных инцидента`);
// Line 299: issues.push(`${slaBreaches.length} SLA нарушений за 24ч`);
// Line 304: issues.push(`${suspiciousIPs.length} подозрительных IP`);
// Line 373: title: `🚨 Чрезвычайная ситуация: ${args.severity}`,

// ──── AFTER (English) ─────────────────────────────────────────
// Line 289:
issues.push(`${enrichedTickets.length} critical tickets`);
// Line 294:
issues.push(`${enrichedIncidents.length} active incidents`);
// Line 299:
issues.push(`${slaBreaches.length} SLA breaches in 24h`);
// Line 304:
issues.push(`${suspiciousIPs.length} suspicious IPs`);
// Line 373:
title: `🚨 Emergency: ${args.severity}`,