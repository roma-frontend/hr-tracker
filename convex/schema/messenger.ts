import { defineTable } from 'convex/server';
import { v } from 'convex/values';

export const messenger = {
  // Messenger tables - if distinct from chat, these would be additional messenger features
  // Currently chat.ts covers the messenger functionality
  // This module is reserved for future messenger-specific tables
};
