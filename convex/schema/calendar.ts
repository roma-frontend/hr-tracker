import { defineTable } from 'convex/server';
import { v } from 'convex/values';

export const calendar = {
  // Calendar tables - calendarAccess is in drivers.ts
  // This module is reserved for future calendar-specific tables
};
