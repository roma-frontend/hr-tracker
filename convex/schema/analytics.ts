import { defineTable } from 'convex/server';
import { v } from 'convex/values';

export const analytics = {
  // Analytics tables - currently covered by other modules
  // This module is reserved for future analytics-specific tables
};
