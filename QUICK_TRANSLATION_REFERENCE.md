# 🚀 Quick Translation Reference

## Быстрый доступ к переводам

### 🌍 Поддерживаемые языки
- 🇬🇧 **en** - English (325 keys)
- 🇦🇲 **hy** - Հայերեն (325 keys)
- 🇷🇺 **ru** - Русский (325 keys)

---

## 📖 Часто используемые переводы

### Common Actions (common.*)
```tsx
t('common.save')         // Сохранить / Պահպանել / Save
t('common.cancel')       // Отмена / Չեղարկել / Cancel
t('common.delete')       // Удалить / Ջնջել / Delete
t('common.edit')         // Редактировать / Խմբագրել / Edit
t('common.submit')       // Отправить / Ուղարկել / Submit
t('common.search')       // Поиск / Փնտրել / Search
t('common.loading')      // Загрузка... / Բեռնվում է... / Loading...
t('common.confirm')      // Подтвердить / Հաստատել / Confirm
```

### Navigation (nav.*)
```tsx
t('nav.dashboard')       // Панель / Վահանակ / Dashboard
t('nav.employees')       // Сотрудники / Աշխատակիցներ / Employees
t('nav.leaves')          // Отпуска / Արձակուրդներ / Leaves
t('nav.attendance')      // Посещаемость / Ներկայություն / Attendance
t('nav.settings')        // Настройки / Կարգավորումներ / Settings
t('nav.profile')         // Профиль / Պրոֆիլ / Profile
```

### Authentication (auth.*)
```tsx
t('auth.login')          // Вход / Մուտք / Login
t('auth.email')          // Эл. почта / Էլ. հասցե / Email
t('auth.password')       // Пароль / Գաղտնաբառ / Password
t('auth.signIn')         // Войти / Մուտք գործել / Sign In
t('auth.signUp')         // Зарегистрироваться / Գրանցվել / Sign Up
```

### Leave Management (leave.*)
```tsx
t('leave.requestLeave')  // Запросить отпуск / Արձակուրդի հայտ / Request Leave
t('leave.pending')       // Ожидание / Սպասվող / Pending
t('leave.approved')      // Утвержден / Հաստատված / Approved
t('leave.rejected')      // Отклонен / Մերժված / Rejected
t('leave.annual')        // Ежегодный отпуск / Տարեկան արձակուրդ / Annual Leave
t('leave.sick')          // Больничный / Հիվանդության արձակուրդ / Sick Leave
```

### Employees (employees.*)
```tsx
t('employees.addEmployee')      // Добавить сотрудника / Ավելացնել աշխատակից / Add Employee
t('employees.totalEmployees')   // Всего сотрудников / Ընդամենը աշխատակիցներ / Total Employees
t('employees.active')           // Активен / Ակտիվ / Active
```

### Notifications (notifications.*)
```tsx
t('notifications.leaveApproved')  // Отпуск утвержден / Արձակուրդը հաստատվել է / Leave Approved
t('notifications.leaveRejected')  // Отпуск отклонен / Արձակուրդը մերժվել է / Leave Rejected
```

### Errors (errors.*)
```tsx
t('errors.required')        // Обязательное поле / Այս դաշտը պարտադիր է / Required
t('errors.invalidEmail')    // Неверный email / Սխալ էլ. հասցե / Invalid email
```

### Success (success.*)
```tsx
t('success.saved')      // Сохранено / Պահպանվել է / Saved
t('success.updated')    // Обновлено / Թարմացվել է / Updated
t('success.deleted')    // Удалено / Ջնջվել է / Deleted
```

---

## 🎯 Параметризация

### С переменными
```tsx
// Перевод с параметрами
t('leave.daysRemaining', { days: 10 })

// English: "10 days remaining"
// Armenian: "10 օր մնացել է"
// Russian: "10 дн. осталось"
```

```tsx
// Множество параметров
t('notifications.newLeaveRequest', { name: 'Иван Иванов' })

// English: "New leave request from Иван Иванов"
// Armenian: "Նոր արձակուրդի հայտ Иван Иванов-ից"
// Russian: "Новый запрос на отпуск от Иван Иванов"
```

---

## 🔧 Использование в компонентах

### Базовое использование
```tsx
import { useTranslation } from 'react-i18next';

function MyComponent() {
  const { t } = useTranslation();
  
  return <h1>{t('dashboard.welcome')}</h1>;
}
```

### Переключение языка
```tsx
import { useTranslation } from 'react-i18next';

function LanguageButtons() {
  const { i18n } = useTranslation();
  
  return (
    <>
      <button onClick={() => i18n.changeLanguage('en')}>English</button>
      <button onClick={() => i18n.changeLanguage('hy')}>Հայերեն</button>
      <button onClick={() => i18n.changeLanguage('ru')}>Русский</button>
    </>
  );
}
```

### Получение текущего языка
```tsx
const { i18n } = useTranslation();
const currentLang = i18n.language; // 'en', 'hy', или 'ru'
```

---

## 📋 Полный список разделов

| Раздел | Префикс | Ключей | Описание |
|--------|---------|--------|----------|
| Landing | `landing.*` | 21 | Лендинг страница |
| Common | `common.*` | 60 | Общие элементы |
| Navigation | `nav.*` | 19 | Меню навигации |
| Auth | `auth.*` | 20 | Аутентификация |
| Dashboard | `dashboard.*` | 21 | Главная панель |
| Leave | `leave.*` | 39 | Отпуска |
| Attendance | `attendance.*` | 30 | Посещаемость |
| Employees | `employees.*` | 35 | Сотрудники |
| Reports | `reports.*` | 21 | Отчеты |
| Settings | `settings.*` | 27 | Настройки |
| Notifications | `notifications.*` | 14 | Уведомления |
| Errors | `errors.*` | 10 | Ошибки |
| Success | `success.*` | 8 | Успешные операции |

---

## 🔍 Поиск переводов

### В VSCode
1. Открыть поиск: `Ctrl + Shift + F`
2. Искать ключ, например: `"requestLeave"`
3. Найти в файлах: `src/i18n/locales/*.json`

### Структура файлов
```
src/i18n/locales/
├── en.json (11.44 KB) - English
├── hy.json (16.70 KB) - Armenian
└── ru.json (15.86 KB) - Russian
```

---

## 📚 Документация

- **Полное руководство:** [TRANSLATIONS_README.md](./TRANSLATIONS_README.md)
- **Детальный гид:** [TRANSLATION_GUIDE.md](./TRANSLATION_GUIDE.md)
- **Отчет о завершении:** [TRANSLATION_COMPLETION_REPORT.md](./TRANSLATION_COMPLETION_REPORT.md)

---

## 💡 Tips

1. **Всегда используйте t()** - Не хардкодьте текст, используйте переводы
2. **Проверяйте все языки** - Тестируйте интерфейс на всех 3 языках
3. **Используйте параметры** - Для динамических значений используйте `{{ }}`
4. **Консистентность** - Используйте одинаковые термины для одинаковых действий
5. **Контекст важен** - Переводите с учетом контекста использования

---

**Последнее обновление:** 28.02.2026  
**Статус:** ✅ Все переводы завершены на 100%
