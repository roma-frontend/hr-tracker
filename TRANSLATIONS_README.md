# 🌍 Многоязычность / Multilingual Support

## Поддерживаемые языки

Приложение поддерживает **3 языка**:

| Язык | Код | Флаг | Статус | Ключей |
|------|-----|------|--------|--------|
| English | `en` | 🇬🇧 | ✅ Завершен | 325 |
| Հայերեն (Armenian) | `hy` | 🇦🇲 | ✅ Завершен | 325 |
| Русский (Russian) | `ru` | 🇷🇺 | ✅ Завершен | 325 |

## 📁 Структура файлов

```
src/
├── i18n/
│   ├── config.ts              # Конфигурация i18next
│   └── locales/
│       ├── en.json            # Английский
│       ├── hy.json            # Армянский
│       └── ru.json            # Русский (НОВЫЙ!)
├── components/
│   ├── I18nProvider.tsx       # Провайдер переводов
│   └── LanguageSwitcher.tsx   # Переключатель языков
```

## 🚀 Быстрый старт

### Использование в компонентах

```tsx
import { useTranslation } from 'react-i18next';

function MyComponent() {
  const { t } = useTranslation();
  
  return (
    <div>
      <h1>{t('dashboard.welcome')}</h1>
      <p>{t('common.loading')}</p>
    </div>
  );
}
```

### Переключение языка

```tsx
import { useTranslation } from 'react-i18next';

function LanguageSelector() {
  const { i18n } = useTranslation();
  
  const changeLanguage = (lang: string) => {
    i18n.changeLanguage(lang);
  };
  
  return (
    <div>
      <button onClick={() => changeLanguage('en')}>English</button>
      <button onClick={() => changeLanguage('hy')}>Հայերեն</button>
      <button onClick={() => changeLanguage('ru')}>Русский</button>
    </div>
  );
}
```

## 📦 Разделы переводов

### 1. `landing` - Лендинг (21 ключ)
```tsx
t('landing.heroTitle')        // Заголовок
t('landing.features')         // Возможности
t('landing.getStarted')       // Начать
```

### 2. `common` - Общие (60 ключей)
```tsx
t('common.save')             // Сохранить
t('common.cancel')           // Отмена
t('common.loading')          // Загрузка...
```

### 3. `nav` - Навигация (19 ключей)
```tsx
t('nav.dashboard')           // Панель
t('nav.employees')           // Сотрудники
t('nav.settings')            // Настройки
```

### 4. `auth` - Аутентификация (20 ключей)
```tsx
t('auth.login')              // Вход
t('auth.email')              // Эл. почта
t('auth.password')           // Пароль
```

### 5. `dashboard` - Панель управления (21 ключ)
```tsx
t('dashboard.welcome')       // Добро пожаловать
t('dashboard.quickActions')  // Быстрые действия
```

### 6. `leave` - Отпуска (39 ключей)
```tsx
t('leave.requestLeave')      // Запросить отпуск
t('leave.pending')           // Ожидание
t('leave.approved')          // Утвержден
```

### 7. `attendance` - Посещаемость (30 ключей)
```tsx
t('attendance.checkIn')      // Отметиться
t('attendance.present')      // Присутствует
```

### 8. `employees` - Сотрудники (35 ключей)
```tsx
t('employees.addEmployee')   // Добавить сотрудника
t('employees.active')        // Активен
```

### 9. `reports` - Отчеты (21 ключ)
```tsx
t('reports.generateReport')  // Создать отчет
t('reports.exportPDF')       // Экспорт в PDF
```

### 10. `settings` - Настройки (27 ключей)
```tsx
t('settings.profile')        // Профиль
t('settings.language')       // Язык
t('settings.theme')          // Тема
```

### 11. `notifications` - Уведомления (14 ключей)
```tsx
t('notifications.title')              // Уведомления
t('notifications.leaveApproved')      // Отпуск утвержден
```

### 12. `errors` - Ошибки (10 ключей)
```tsx
t('errors.required')         // Обязательное поле
t('errors.invalidEmail')     // Неверный email
```

### 13. `success` - Успех (8 ключей)
```tsx
t('success.saved')           // Сохранено
t('success.updated')         // Обновлено
```

## 🔧 Добавление новых переводов

### 1. Добавьте ключ во все файлы локализации

**en.json**
```json
{
  "mySection": {
    "myKey": "My English text"
  }
}
```

**hy.json**
```json
{
  "mySection": {
    "myKey": "Իմ հայերեն տեքստը"
  }
}
```

**ru.json**
```json
{
  "mySection": {
    "myKey": "Мой русский текст"
  }
}
```

### 2. Используйте в компоненте

```tsx
const { t } = useTranslation();
<div>{t('mySection.myKey')}</div>
```

## ✅ Проверка переводов

Запустите тестовый скрипт для проверки синхронизации:

```bash
node tmp_rovodev_test_translations.js
```

Результат:
```
✨ Отлично! Все переводы полностью синхронизированы!
📊 English (en): 325 ключей
📊 Armenian (hy): 325 ключей
📊 Russian (ru): 325 ключей
```

## 🎯 Параметризация

Используйте интерполяцию для динамических значений:

```json
{
  "leave": {
    "daysRemaining": "{{days}} days remaining"
  }
}
```

```tsx
t('leave.daysRemaining', { days: 10 })
// English: "10 days remaining"
// Armenian: "10 օր մնացել է"
// Russian: "10 дн. осталось"
```

## 📚 Документация

- [TRANSLATION_GUIDE.md](./TRANSLATION_GUIDE.md) - Подробное руководство
- [i18next документация](https://www.i18next.com/)
- [react-i18next документация](https://react.i18next.com/)

## 🌟 Особенности

- ✅ Автоопределение языка браузера
- ✅ Сохранение выбора в localStorage
- ✅ Полная поддержка TypeScript
- ✅ SSR-совместимость (Next.js)
- ✅ Переключение без перезагрузки страницы
- ✅ 100% покрытие всех разделов

## 🎨 Переключатель языков

Компонент `LanguageSwitcher` доступен во всех частях приложения:

```tsx
import { LanguageSwitcher } from '@/components/LanguageSwitcher';

<LanguageSwitcher />
```

Отображает:
- 🇬🇧 English
- 🇦🇲 Հայերեն  
- 🇷🇺 Русский

---

**Обновлено:** 2026-02-28  
**Версия:** 1.0.0  
**Статус:** ✅ Все переводы завершены
